#include<stdio.h>
int main()
{
int rad;
float area,ci;
float pi=3.14;
printf("value of radius");
scanf("%d",&rad);
area=pi*rad*rad;
printf("\narea of circle:%f",area);
ci=2*pi*rad;
printf("\ncircumference of circle:%f",ci);
return 0;
}
