#include<stdio.h>
int main()
{
int n,i,ans=0;
printf("enter the n value");
scanf("%d",&n);
if(n<=40)
{
for(i=1;i<=n;i++)
{
ans=ans+(i*i*i*i);
}
printf("%d\n",ans);
}
else
{
printf("not in a limit\n");
}
return 0;
}
