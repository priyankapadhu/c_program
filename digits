#include<stdio.h>
int main()
{
int n,rem,temp,sum=0;
printf("\n the number is :");
scanf("%d",&n);
temp=n;
while(n>0)
{
rem=n%10;
sum=sum+rem;
n/=10;
}
printf("\n sum of the digits %d = %d\n",temp,sum);
return 0;
}
